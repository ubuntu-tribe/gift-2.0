/**
 * One-off wiring script:
 * - supplyController setup
 * - escrow + NFT ownership wiring
 * - marketplace / registry configuration
 */
export async function configureSystem() {
  // TODO: implement
}
